# Bot/bot2 (Lookism Bot v2) dependencies
# Majors pinned to guard against surprise breaking changes on redeploy.
discord.py>=2.3.0,<3
python-dotenv>=1.0.0,<2
aiohttp>=3.9.0,<4
pytest>=8.0.0,<10
firebase-admin>=6.0.0,<8
